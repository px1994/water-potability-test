from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='this is a water potability prediction project',
    author='pritesh',
    license='',
)
